# Source assets

Keep editable master files here. Preserve source formats and attribution or license information
with each asset set. Generated game-ready files belong in `assets/processed/` and are not tracked
unless a future release process deliberately changes that policy.
