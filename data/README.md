# Runtime data

Store shard-authored runtime data here before deployment. Keep content grouped by purpose:

- `decoration/`
- `regions/`
- `spawns/`
- `configuration/`
- `localization/`

The deployment tooling should copy only reviewed files into ModernUO's `Distribution` tree.
